# Copy this file to .env and fill in real values. .env is gitignored.

# Server
PORT=3000
JWT_SECRET=your_jwt_secret_here
DATABASE_URL=postgres://username:password@hostname/database_namewith_a_long_random_string

# Database
# This project uses SQLite (see db.js) - the database file is stored locally
# at ./database.sqlite and created automatically on first run. No DB
# host/user/password is needed.
